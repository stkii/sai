# TODO

## 検討中

- 履歴のフィルタ・ページング (件数が増えたら導入する)
- 任意の変数の作成 (合計得点・計算式など)
- 結果のエクスポート (PDF / CSV)
- 配布パッケージング (R ランタイム同梱と `cli.R` のパス解決)

## 見送ったもの

- **`HistoryService` の撤去** — 全メソッドが `HistoryStore` への 1 行素通しだが、
  外すと `commands/history.rs` が infra の具象を直接持つことになり
  「infra の具象は commands に露出しない」という層の規則に反する。
  ロジックではなく境界として残す
- **反復測定分散分析の Type III 化** — 各被験者が全セルを 1 回ずつ持つ計画では
  Type I と一致するため、平方和の型ではなく均衡の崩れを `n_note` で通知する方針とした
