# UX Issues (ベータ版レビュー)

レビュー日: 2026-03-18

心理学研究者が実際に使う観点でのUXレビュー結果。
対象ユーザー: R/pnpm環境を自分で構築し `pnpm start` から始めるベータユーザー。

---

## 対応予定

### 1. 結果のエクスポート機能が未実装

**重要度: CRITICAL**

分析結果をコピー・エクスポートする手段が一切無い。`MethodModule.buildExportSections` と `rust_xlsxwriter` のインフラは揃っているがUIが繋がっていない。

最低限必要な対応:
- テーブルのクリップボードコピー（タブ区切り → Excel貼り付け可能）
- XLSXエクスポート（`rust_xlsxwriter` 活用）

関連ファイル:
- `src/analysis/methods/contracts.ts` — `buildExportSections` インターフェース定義
- `src/analysis/methods/utils.ts` — `buildExportSectionsFromResult` 実装
- 各 `result.tsx` — メソッド別エクスポート関数（現状パススルーのみ）

### 3. ~~データ読み込み後に行数・列数が表示されない~~ ✅ 対応済み

**重要度: HIGH**

`LoadedDatasetBar` にデータの行数・列数を「（N行 × M列）」形式で表示するよう修正。コミット: `c68cb4a`

### 4. ~~分析条件がJSON生表示~~ ✅ 対応済み

**重要度: HIGH**

分析ログ詳細ビューの分析条件を、メソッド別に日本語ラベル付きのリスト表示に変更。データ・変数も同じ枠内に統合。コミット: see git log

### 7. 仮定の検定結果が報告されない

**重要度: MEDIUM**

論文で必要とされる分析前提条件のチェックが報告されない:
- 相関: 正規性検定（Shapiro-Wilk）
- 回帰: Durbin-Watson（※VIF は対応済み）
- ANOVA: 等分散性（Levene検定）
- 因子分析: KMO・Bartlett検定

対応案: R側で検定を実行し、結果に追加セクションとして表示する。

関連ファイル:
- `src-r/R/correlation.R`, `regression.R`, `anova.R`, `factor.R`
- 各 `result.tsx`

### 8. 信頼区間が報告されない（部分対応済み）

**重要度: MEDIUM**

~~効果量・相関係数・回帰係数~~の95%信頼区間（CI）が報告されない。ジャーナルではCI報告が求められる傾向。

- ~~相関係数（Pearson）の CI~~ ✅ 対応済み（Fisher z 変換ベース）
- ~~回帰係数の CI~~ ✅ 対応済み（`confint()` 使用）
- 効果量（偏η²等）の CI — 未実装

残対応: ANOVA 効果量の 90% CI（非心 F 分布ベース）

関連ファイル:
- `src-r/R/anova.R`

### 10. データ切り替え時に確認ダイアログがない

**重要度: MEDIUM**

新しいデータを読み込むと、開いている分析モーダルが無言で閉じ、途中の設定が失われる。

対応案: 分析モーダルが開いている状態でデータを切り替える場合、確認ダイアログを表示する。

関連ファイル:
- `src/windows/DataWindow.tsx:54-63` — `handleLoaded` コールバック

---

## 保留

### 2. 分析オプションに説明が一切無い

**重要度: CRITICAL**

因子分析の回転法（varimax/oblimin/promax等）、ANOVAの効果量、相関の欠損値処理、回帰の中心化オプション等、全ての分析オプションにツールチップや説明テキストが無い。

関連箇所:
- 因子分析: 回転法6種、ガットマン基準 vs 固定、promax power
- ANOVA: 効果量4種（偏η², η², ω², なし）
- 相関: 欠損値の扱い（行ごと vs ペアごと）
- 回帰: 中心化オプション

### 5. スクリープロットがデフォルトOFF

**重要度: HIGH**

`factor/modal.tsx` で `showScreePlot` のデフォルトが `false`。心理学の因子分析ではスクリープロットは必須の判断材料であり、デフォルトONが望ましい。

### 6. テーブルの列ヘッダーが切れてもツールチップがない

**重要度: HIGH**

`DataTable.tsx` のセル幅が固定80pxで長い変数名が途中で切れるが、ホバー時のツールチップが無い。どの変数か確認できないまま分析に進む可能性がある。

### 9. XLSXシートの自動選択が通知なし

**重要度: MEDIUM**

XLSXファイル読み込み時に最初のシートが自動選択される（`DataImportDialog.tsx:58-59`）。複数シートのファイルで誤ったシートを分析するリスク。

### 11. APA形式のテキスト出力

**重要度: LOW**

「r(98) = .45, p < .001」「F(2, 117) = 4.23, p = .017, ηp² = .07」のような形式でコピーできると論文執筆が楽になる。

### 12. 有意性の凡例表示

**重要度: LOW**

結果テーブルに「\*」「\*\*」が付くが「\* p < .05, \*\* p < .01, \*\*\* p < .001」の凡例が表示されない。

### 13. 結果の比較機能

**重要度: LOW**

因子分析で回転法を変えた結果を並べて比較する機能がない。現状は1つの結果しか表示できない。
